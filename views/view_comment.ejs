
<%- include('header') %>


            <div class="main-content">
                <div class="section__content section__content--p30">
                    <div class="container-fluid">
                        <div class="row">
                            <div class="col-lg-12">
                                <form class="form-header"  >
                                    <input class="au-input au-input--xl" type="text" name="search"  placeholder="Search for datas &amp; reports..." />
                                    <button class="au-btn--submit" type="submit" name="searching" >
                                        <i class="zmdi zmdi-search"></i>
                                    </button>
                                </form>
                                <div class="table-responsive table--no-card m-b-30">
                                    <form method="post" action="/slider/multiDelete">
                                    <table class="table table-borderless table-striped table-earning">
                                        <thead>
                                            <tr>
                                                <th><input type="checkbox" id="allCheckBox">
                                                <button type="submit" style="color:white"><i class="fa fa-trash"></i></button></th>
                                                <th>No</th>
                                                <th>Post Name</th>
                                                <th>Name</th>
                                                <th>Email</th>
                                                <th>comment</th>
                                                <th>image</th>
                                                <th>Status</th>
                                                <th>Action</th>
                                            </tr>
                                        </thead>
                                        <tbody>
                                            <% for(var ad of cData){ %>
                                            <tr>
                                                <td><input type="checkbox" value="<%= ad.id %>" name="multiDelete[]" class="checkBoxClass" /></td>
                                                <td>1</td>
                                                <td><%= ad.postId.title %></td>
                                                <td><%= ad.name %></td>
                                                <td><%= ad.email %></td>
                                                <td><%= ad.comment %></td>
                                                <td><img src="<%= ad.image %>" height="50" width="50" ></td>
                                                <td>
                                                    <% if(ad.isActive){ %>
                                                      <a href="?id=<%= ad.id %>&status=deactive">Active</a>
                                                    <% } else { %>
                                                        <a href="?id=<%= ad.id %>&status=active">Deactive</a>
                                                    <% } %>
                                                </td>
                                                <td>Delete || Update</td>
                                            </tr>
                                            <% } %>
                                            <tr>
                                                
                                            </tr>
                                       
                                        </tbody>
                                    </table>
                                </form>
                                </div>
                            </div>
                          
                        </div>
                        
                        <%- include('footer') %>